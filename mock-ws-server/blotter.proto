syntax = "proto3";

message BlotterEntry {
  int32 id = 1;
  string ticker = 2;
  double qty = 3;
  double price = 4;
  string status = 5;
}

message BlotterBatch {
  repeated BlotterEntry entries = 1;
}
